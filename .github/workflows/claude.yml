name: Claude Bot Action
on:
  issue_comment:
    types: [created]
jobs:
  run-claude:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
    steps:
      - uses: actions/checkout@v4
      - uses: anthropics/claude-code-action@main
        with:
          anthropic_api_key: "sk-ant-dummy-key"  # API Key Anthropic boleh tetap palsu, tidak akan dicek di awal
          github_token: ${{ secrets.GITHUB_TOKEN }} # Menggunakan token asli bawaan runner
